import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";

import MailIcon from "@mui/icons-material/Mail";

import { SCFTooltip, SCFDivider } from "../../components";

const SCFTooltipDemo = () => {
  
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Tooltip
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Grid container spacing={2} p={4} direction="column">
        <Grid item alignContent={"center"}>
          <SCFTooltip title='mail'placement="bottom-start">
            <MailIcon color="action" />
          </SCFTooltip>
        </Grid>
      </Grid>
    </>
  );
};

export default SCFTooltipDemo;