import React from "react";

import Typography from "@mui/material/Typography";

import { SCFBreadcrumbs, SCFDivider } from "../../components";
import { Link } from "@mui/material";

const SCFBreadcrumbsDemo = () => {
  function handleClickB(event: React.MouseEvent<HTMLDivElement, MouseEvent>) {
    event.preventDefault();
  }
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Breadcrumbs
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <div role="presentation" onClick={handleClickB}>
        <SCFBreadcrumbs aria-label="breadcrumb">
          <Link underline="hover" color="inherit" href="/">
            MUI
          </Link>
          <Link underline="hover" color="inherit" href="/">
            Core
          </Link>
          <Typography color="text.primary">Breadcrumbs</Typography>
        </SCFBreadcrumbs>
      </div>
    </>
  );
};

export default SCFBreadcrumbsDemo;
