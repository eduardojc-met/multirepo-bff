import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import FormGroup from "@mui/material/FormGroup";
import FormControlLabel from "@mui/material/FormControlLabel";

import { SCFCheckbox, SCFDivider } from "../../components";

const SCFCheckboxDemo = () => {
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Checkbox
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Grid container spacing={2} p={4} direction="column">
        <FormGroup>
          <FormControlLabel
            control={<SCFCheckbox defaultChecked />}
            label="Label"
          />
          <FormControlLabel
            disabled
            control={<SCFCheckbox />}
            label="Disabled"
          />
        </FormGroup>
      </Grid>
    </>
  );
};

export default SCFCheckboxDemo;
