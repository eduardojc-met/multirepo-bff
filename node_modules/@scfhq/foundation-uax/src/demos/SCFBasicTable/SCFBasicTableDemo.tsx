import React from "react";

import Typography from "@mui/material/Typography";


import { SCFBasicTable, SCFDivider } from "../../components";
import { bodyData, headerData } from "./SCFBasicTableData";

const SCFBasicTableDemo = () => {
  const handleDetails = (row: object) => {
    alert(`'Vas a ver los detalles', ${row}`);
  };
  const handleDelete = (row: object) => {
    alert(`'Vas a borrar ' , ${row}`);
  };
  const handleEdit = (row: object) => {
    alert(`'Vas a editar', ${row}`);
  };
  const handleAdd = () => {
    alert("Vas a añadir");
  };
  const handleActive = (row: object) => {
    alert(`'Vas a reactivar a este usuario' ${row}'`);
  };
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        BasicTable
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <SCFBasicTable
        arrayHeader={headerData}
        arrayBody={bodyData}
        handleDetails={handleDetails}
        handleDelete={handleDelete}
        handleEdit={handleEdit}
        handleAdd={handleAdd}
        handleActive={handleActive}
      />
    </>
  );
};

export default SCFBasicTableDemo;
