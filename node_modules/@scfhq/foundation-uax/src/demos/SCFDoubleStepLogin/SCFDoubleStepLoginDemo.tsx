import React, {  } from "react";


import { SCFDoubleStepLogin } from "../../components";

const SCFDoubleStepLoginDemo = () => {

  // Test. Do nothing
  const handleLogin = (props: any) => {
    alert(JSON.stringify(props));
  };

  // Test. Always true
  const handlePreAuth = (): boolean => {
    return true;
  };

  return (
    <SCFDoubleStepLogin
      onLookup={handlePreAuth}
      onSubmit={handleLogin}
      forgotPasswordUrl={"https://www.santanderconsumer.es/"}
      loginError={false}
    />
  );
};

export default SCFDoubleStepLoginDemo;
