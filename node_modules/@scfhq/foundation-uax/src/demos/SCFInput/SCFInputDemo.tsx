import React from "react";

import Typography from "@mui/material/Typography";

import { SCFDivider, SCFInput } from "../../components";
import { Box } from "@mui/material";

const SCFInputDemo = () => {
  
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        SCFInput
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Box
        component="form"
        sx={{
          "& > :not(style)": { m: 1, width: "25ch" },
        }}
        noValidate
        autoComplete="off"
      >
        <SCFInput name="outlined-basic" label="Outlined" variant="outlined" />
        <SCFInput name="filled-basic" label="Filled" variant="filled" />
        <SCFInput name="standard-basic" label="Standard" variant="standard" />
      </Box>
    </>
  );
};

export default SCFInputDemo;