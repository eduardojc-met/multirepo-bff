import React from "react";
import Typography from "@mui/material/Typography";


import { SCFLinks, SCFDivider } from "../../components";
import { Box } from "@mui/material";

const SCFLinksDemo = () => {
  const preventDefault = (event: { preventDefault: () => any }) =>
    event.preventDefault();
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Links
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Box
        sx={{
          typography: "body1",
          "& > :not(style) + :not(style)": {
            ml: 2,
          },
        }}
        onClick={preventDefault}
      >
        <SCFLinks href="#">Link</SCFLinks>
        <SCFLinks href="#" color="inherit">
          {'color="inherit"'}
        </SCFLinks>
        <SCFLinks href="#" variant="body2">
          {'variant="body2"'}
        </SCFLinks>
      </Box>
    </>
  );
};

export default SCFLinksDemo;