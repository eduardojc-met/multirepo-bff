import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";

import { SCFBasicPagination, SCFDivider } from "../../components";
import Stack from "@mui/material/Stack";

const SCFBasicPaginationDemo = () => {
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Basic Pagination
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Grid container spacing={2} p={4} direction="column">
        <Grid item alignContent={"center"}>
          <Stack spacing={2}>
            <SCFBasicPagination count={10} />
            <SCFBasicPagination count={10} color="primary" />
            <SCFBasicPagination count={10} color="secondary" />
            <SCFBasicPagination count={10} disabled />
          </Stack>
        </Grid>
      </Grid>
    </>
  );
};

export default SCFBasicPaginationDemo;
