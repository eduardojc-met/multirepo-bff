import React, {  } from "react";
import { SCFLogin } from "../../components";

const SCFLoginDemo = () => {

  // Test. Do nothing
  const handleLogin = (props: any) => {
    alert(JSON.stringify(props));
  };

  return (
    <SCFLogin
        onSubmit={handleLogin}
        forgotPassword={() => {alert('forgot password')}}
        loginError={false}
        rememberMe={false}
        recaptcha={true}
        // Pasamos esta función y el token para que se muestre el captcha
        evaluateRecaptchaToken={() => new Promise(() => true)}
        siteKey='6LcJsB8eAAAAAEHl8gmAG6RYRqwSM-kaWYzxUb0r'
        /**Esta es la key de centen, se puede utilizar para pruebas,
         *const siteKey= '6LcJsB8eAAAAAEHl8gmAG6RYRqwSM-kaWYzxUb0r';
        **/
      />
  );
};

export default SCFLoginDemo;
