import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";
import BottomNavigationAction from "@mui/material/BottomNavigationAction";

import RestoreIcon from "@mui/icons-material/Restore";
import LocationOnIcon from "@mui/icons-material/LocationOn";
import FavoriteIcon from "@mui/icons-material/Favorite";

import { SCFBottomNavigation, SCFDivider } from "../../components";

const SCFBottomNavigationDemo = () => {
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Bottom Navigation
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Grid container spacing={2} p={4} direction="column">
        <Grid item alignContent={"center"}>
          <Box sx={{ width: 500 }}>
            <SCFBottomNavigation showLabels>
              <BottomNavigationAction label="Recents" icon={<RestoreIcon />} />
              <BottomNavigationAction
                label="Favorites"
                icon={<FavoriteIcon />}
              />
              <BottomNavigationAction
                label="Nearby"
                icon={<LocationOnIcon />}
              />
            </SCFBottomNavigation>
          </Box>
        </Grid>
      </Grid>
    </>
  );
};

export default SCFBottomNavigationDemo;
