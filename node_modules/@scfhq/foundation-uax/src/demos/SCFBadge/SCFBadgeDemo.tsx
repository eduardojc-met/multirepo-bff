import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";

import MailIcon from "@mui/icons-material/Mail";

import { SCFBadge, SCFDivider } from "../../components";

const SCFBadgeDemo = () => {
  
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Badge
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Grid container spacing={2} p={4} direction="column">
        <Grid item alignContent={"center"}>
          <SCFBadge badgeContent={4} color="secondary">
            <MailIcon color="action" />
          </SCFBadge>
          <SCFBadge badgeContent={4} color="success">
            <MailIcon color="action" />
          </SCFBadge>
        </Grid>
      </Grid>
    </>
  );
};

export default SCFBadgeDemo;
