import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import Stack from "@mui/material/Stack";

import { SCFButton, SCFDivider } from "../../components";

const SCFButtonDemo = () => {

  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Button
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Grid container spacing={2} p={4} direction="column">
        <Grid item alignContent={"center"}>
          <Stack spacing={2} direction="row">
            <SCFButton>Default button</SCFButton>
            <Typography variant="bodySmall">
              A la derecha hay un botón no debe renderizarse porque tiene
              show=false
            </Typography>
            <SCFButton show={false}>Botón que no debe renderizarse</SCFButton>
          </Stack>
        </Grid>
        <Grid item>
          <Stack spacing={2} direction="row">
            <SCFButton variant="text">Text</SCFButton>
            <SCFButton variant="contained">Contained</SCFButton>
            <SCFButton variant="outlined">Outlined</SCFButton>
            <SCFButton variant="contained" disabled>
              Disabled
            </SCFButton>
            <SCFButton variant="contained" size="small">
              Small
            </SCFButton>
            <SCFButton variant="contained" size="large">
              Large
            </SCFButton>
            <SCFButton variant="contained" color="secondary">
              Secondary
            </SCFButton>
          </Stack>
        </Grid>
      </Grid>
    </>
  );
};

export default SCFButtonDemo;
