import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import FormControlLabel from "@mui/material/FormControlLabel";

import { SCFRadioGroup, SCFDivider, SCFRadio } from "../../components";

const SCFRadioGroupDemo = () => {
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        RadioGroup
      </Typography>
      <SCFDivider />
      <Grid item alignContent={"left"}>
        <SCFRadioGroup
          aria-labelledby="demo-radio-buttons-group-label"
          defaultValue="female"
          name="radio-buttons-group"
        >
          <FormControlLabel
            value="female"
            control={<SCFRadio />}
            label="Female"
          />
          <FormControlLabel value="male" control={<SCFRadio />} label="Male" />
          <FormControlLabel
            value="other"
            control={<SCFRadio />}
            label="Other"
          />
        </SCFRadioGroup>
      </Grid>
    </>
  );
};

export default SCFRadioGroupDemo;
