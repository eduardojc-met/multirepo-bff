import React from "react";

import Typography from "@mui/material/Typography";
import { SCFMiniDrawer, SCFDivider } from "../../components";
import { Box } from "@mui/material";

import { menuData, menuData2 } from "./SCFMiniDrawerData";


const SCFMiniDrawerDemo = () => {
  
  return (
    <>
      <Box>
        <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
          MiniDrawer
        </Typography>
        <SCFDivider sx={{ mb: 4 }} />
        <SCFMiniDrawer
          show={false}
          arrayMenu={menuData}
          arrayMenu2={menuData2}
        />
      </Box>
    </>
  );
};

export default SCFMiniDrawerDemo;