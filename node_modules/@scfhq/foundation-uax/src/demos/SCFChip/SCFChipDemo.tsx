import React from "react";
import Typography from "@mui/material/Typography";

import { SCFChip, SCFDivider } from "../../components";
import Stack from "@mui/material/Stack";

const SCFChipDemo = () => {
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Chip
      </Typography>
      <SCFDivider sx={{ mb: 4 }} />
      <Stack direction="row" spacing={1}>
        <SCFChip label="Chip Filled" />
        <SCFChip label="Chip Outlined" variant="outlined" />
      </Stack>
    </>
  );
};

export default SCFChipDemo;
