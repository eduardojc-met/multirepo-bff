import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";

import { SCFSkeleton, SCFDivider } from "../../components";
import Stack from "@mui/material/Stack";

const SCFSkeletonDemo = () => {
  return (
    <>
      <Typography variant="h1" gutterBottom fontWeight={"bold"} sx={{ mt: 8 }}>
        Skeleton
      </Typography>
      <SCFDivider />
      <Grid>
        <Stack spacing={2} direction="column">
          <SCFSkeleton variant="text" />
          <SCFSkeleton variant="circular" width={40} height={40} />
          <SCFSkeleton variant="rectangular" width={210} height={118} />
        </Stack>
      </Grid>
    </>
  );
};

export default SCFSkeletonDemo;
