import React from "react";
export declare type LinkType = {
    to: string;
    icon?: string;
    text: React.ReactNode;
    submenus?: LinkType[];
};
declare type SCFSidebarProps = {
    show?: boolean;
    open: boolean;
    links: LinkType[];
};
declare const SCFSidebar: ({ show, open, links, }: SCFSidebarProps) => JSX.Element;
export default SCFSidebar;
