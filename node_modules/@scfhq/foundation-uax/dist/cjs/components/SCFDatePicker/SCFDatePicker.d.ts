/// <reference types="react" />
import { TextFieldProps } from "@mui/material";
import { RegisterOptions, UseFormReturn } from "react-hook-form";
declare type SCFDatePickerProps = TextFieldProps & {
    /**
     * Controls if the component must be rendered
     */
    show?: boolean;
    max?: number;
    min?: number;
    /**
     * React-hook form. If defined, the component will render a React-hook form controller.
     */
    form?: UseFormReturn;
    /**
     * React-hook-forms rules.
     */
    rules?: Omit<RegisterOptions, "valueAsNumber" | "valueAsDate" | "setValueAs" | "disabled">;
};
declare const SCFDatePicker: ({ show, max, min, variant, form, rules, ...muiprops }: SCFDatePickerProps) => JSX.Element;
export default SCFDatePicker;
