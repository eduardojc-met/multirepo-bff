import React from "react";
import Badge from "@mui/material/Badge";
const SCFBadge = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Badge, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFBadge;
