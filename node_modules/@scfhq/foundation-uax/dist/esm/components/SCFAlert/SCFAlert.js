import React from 'react';
import Alert from '@mui/material/Alert';
const SCFAlert = ({ show = true, ...muiprops }) => {
    return show ? React.createElement(Alert, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFAlert;
