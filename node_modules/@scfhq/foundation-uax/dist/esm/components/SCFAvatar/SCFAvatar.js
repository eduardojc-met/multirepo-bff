import React from "react";
import Avatar from "@mui/material/Avatar";
const SCFAvatar = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Avatar, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFAvatar;
