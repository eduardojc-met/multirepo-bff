import React from 'react';
import Snackbar from '@mui/material/Snackbar';
const SCFSnackbar = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Snackbar, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFSnackbar;
