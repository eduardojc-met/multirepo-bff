import * as React from 'react';
import { DataGrid, GridToolbarColumnsButton, GridToolbarContainer, GridToolbarDensitySelector, GridToolbarFilterButton, esES, enUS } from "@mui/x-data-grid";
import Button from "@mui/material/Button";
import FileDownloadOutlinedIcon from '@mui/icons-material/FileDownloadOutlined';
import SCFGridToolbarExport from "./SCFGridToolbarExport";
const SCFDataGrid = ({ show = true, customExportAction = undefined, customToolbar = undefined, lang = 'en', locale = enUS, ...muiprops }) => {
    const dictionary = {
        es: esES.components.MuiDataGrid.defaultProps.localeText,
        en: enUS.components.MuiDataGrid.defaultProps.localeText
    };
    // Merged custom translations with mui default ones
    const localTranslation = {
        ...dictionary[lang],
        ...locale
    };
    function SCFGRidToolbar() {
        return (React.createElement(GridToolbarContainer, null,
            React.createElement(GridToolbarColumnsButton, null),
            React.createElement(GridToolbarFilterButton, null),
            React.createElement(GridToolbarDensitySelector, null),
            customExportAction ?
                React.createElement(Button, { startIcon: React.createElement(FileDownloadOutlinedIcon, null), onClick: customExportAction }, localTranslation.toolbarExport)
                :
                    React.createElement(SCFGridToolbarExport, null)));
    }
    return show === true ? React.createElement(DataGrid, { ...muiprops, localeText: localTranslation, components: { Toolbar: customToolbar ? customToolbar : SCFGRidToolbar } }) : React.createElement(React.Fragment, null);
};
export default SCFDataGrid;
