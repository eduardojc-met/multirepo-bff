import React from "react";
import Button from "@mui/material/Button";
const SCFButton = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Button, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFButton;
