import * as React from 'react';
import ToggleButton from '@mui/material/ToggleButton';
const SCFToggleButton = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(ToggleButton, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFToggleButton;
