import React from "react";
import Menu from "@mui/material/Menu";
const SCFBasicMenu = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Menu, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFBasicMenu;
