import React from "react";
import Stepper from "@mui/material/Stepper";
const SCFHorizontalStepper = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Stepper, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFHorizontalStepper;
