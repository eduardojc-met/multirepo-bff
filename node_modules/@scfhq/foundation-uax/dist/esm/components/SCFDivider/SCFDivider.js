import React from "react";
import Divider from "@mui/material/Divider";
const SCFDivider = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Divider, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFDivider;
