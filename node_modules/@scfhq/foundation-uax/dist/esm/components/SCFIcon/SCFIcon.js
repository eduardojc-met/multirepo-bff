import React from "react";
/**
 * @deprecated Use SCFIconButton instead
 */
const SCFIcon = ({ className = "", icon = "" }) => {
    return (React.createElement("div", { className: `${className}` },
        React.createElement("i", { className: `${icon}` })));
};
export default SCFIcon;
