import React from "react";
import SCFDialog from "../SCFDialog/SCFDialog";
;
/**
 * @deprecated Use SCFDialog instead
 */
const SCFModal = ({ show = true, modalTitle = undefined, modalDescription = undefined, body = undefined, handleModalClose = () => {
}, ...muiprops }) => {
    return (React.createElement(SCFDialog, { open: muiprops.open, onClose: muiprops.onClose, title: modalTitle, content: body ? body : modalDescription }));
};
export default SCFModal;
