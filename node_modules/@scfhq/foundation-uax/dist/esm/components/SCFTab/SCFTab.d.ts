import React from "react";
import { TabProps } from "@mui/material/Tab";
interface TabPanelProps {
    children?: React.ReactNode;
    index: number;
    value: number;
}
declare type TabConfig = {
    title: string;
    content: React.ReactNode;
};
export declare const TabPanel: (props: TabPanelProps) => JSX.Element;
export declare const a11yProps: (index: number) => {
    id: string;
    "aria-controls": string;
};
declare type SCFTabProps = TabProps & {
    /**
     * Controls if the component must be rendered
     */
    show?: boolean;
    textColor?: "inherit" | "primary" | "secondary";
    indicatorColor?: "primary" | "secondary";
    pages?: TabConfig[];
};
declare const SCFTab: ({ show, textColor, indicatorColor, pages, }: SCFTabProps) => JSX.Element;
export default SCFTab;
