import React from "react";
import Dialog from "@mui/material/Dialog";
import IconButton from "@mui/material/IconButton";
import DialogTitle from "@mui/material/DialogTitle";
import DialogContent from "@mui/material/DialogContent";
import DialogActions from "@mui/material/DialogActions";
import SCFButton from "../SCFButton/SCFButton";
import CloseIcon from "@mui/icons-material/Close";
const SCFDialog = ({ show = true, title = undefined, actionsClassName, content = undefined, actionButtons = undefined, buttons = undefined, onClose = undefined, ...muiprops }) => {
    /**
     * Esto es para darle soporte a la prop buttons, deprecada.
     * TODO: Adaptar cuando se quite la prop button.
     */
    const actions = actionButtons ? actionButtons : buttons;
    return (React.createElement(React.Fragment, null, show && (React.createElement(Dialog, { ...muiprops },
        React.createElement(DialogTitle, { sx: { p: 4 } },
            title,
            onClose && (React.createElement(IconButton, { "aria-label": "close", onClick: onClose, sx: {
                    position: 'absolute',
                    right: 16,
                    top: 12,
                } },
                React.createElement(CloseIcon, null)))),
        React.createElement(DialogContent, { sx: { px: 4, pb: actions ? 2 : 4 } }, content),
        actions && actions.length > 0 && (React.createElement(DialogActions, { sx: { px: 4, pb: 2 } }, actions.map((button, index) => {
            return (React.createElement(SCFButton, { key: index, variant: button.variant, onClick: button.onClick, color: "primary" }, button.title));
        })))))));
};
export default SCFDialog;
