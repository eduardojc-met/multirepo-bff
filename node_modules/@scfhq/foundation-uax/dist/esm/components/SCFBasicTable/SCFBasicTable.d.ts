/// <reference types="react" />
import { TableProps } from "@mui/material/Table";
export declare type SCFBasicTableProps = TableProps & {
    /**
     * Controls if the component must be rendered
     */
    show?: boolean;
    arrayHeader: string[];
    arrayBody: any[];
    width?: string | any;
    editable?: boolean;
    deletable?: boolean;
    details?: boolean;
    inactive?: boolean;
    tableTitle?: string;
    actionLabel?: string;
    action?: boolean;
    stickyHeader?: boolean;
    handleDelete?: (row: any) => void;
    handleEdit?: (row: any) => void;
    handleDetails?: (row: any) => void;
    handleAdd?: () => void;
    handleActive?: (row: any) => void;
    add?: boolean;
    labelRowsPerPage?: string;
};
export declare const SCFBasicTable: ({ show, arrayHeader, arrayBody, width, tableTitle, actionLabel, stickyHeader, handleActive, handleDetails, handleDelete, handleEdit, handleAdd, add, labelRowsPerPage, ...muiprops }: SCFBasicTableProps) => JSX.Element;
export default SCFBasicTable;
