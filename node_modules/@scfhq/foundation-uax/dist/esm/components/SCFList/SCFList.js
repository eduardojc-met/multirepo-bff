import React from "react";
import List from "@mui/material/List";
const SCFList = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(List, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFList;
