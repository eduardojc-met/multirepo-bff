import React from "react";
import Chip from "@mui/material/Chip";
const SCFChip = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Chip, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFChip;
