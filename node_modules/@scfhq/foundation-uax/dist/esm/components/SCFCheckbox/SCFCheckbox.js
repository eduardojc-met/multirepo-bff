import React from "react";
import Checkbox from "@mui/material/Checkbox";
const SCFCheckbox = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Checkbox, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFCheckbox;
