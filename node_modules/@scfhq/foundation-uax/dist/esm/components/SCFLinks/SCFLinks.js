import React from "react";
import Link from "@mui/material/Link";
const SCFLinks = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Link, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFLinks;
