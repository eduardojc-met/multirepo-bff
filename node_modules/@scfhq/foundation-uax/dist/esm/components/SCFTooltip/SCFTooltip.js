import React from 'react';
import Tooltip from '@mui/material/Tooltip';
const SCFTooltip = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Tooltip, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFTooltip;
