import React from 'react';
import IconButton from '@mui/material/IconButton';
const SCFIconButton = ({ show = true, ...muiprops }) => {
    return show ? React.createElement(IconButton, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFIconButton;
