import React from "react";
import Pagination from "@mui/material/Pagination";
const SCFBasicPagination = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Pagination, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFBasicPagination;
