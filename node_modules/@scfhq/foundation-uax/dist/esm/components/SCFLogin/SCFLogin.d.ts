/// <reference types="react" />
import { AxiosResponse } from "axios";
export declare type SCFLoginProps = {
    /**
     * Required. Callback func for submit
     */
    onSubmit: (email: string, pwd: string, rememberMe: boolean) => void;
    /**
     * Check if login has auth error
     */
    loginError: boolean;
    /**
     * Set if component is rendered
     */
    show?: boolean;
    maxWidth?: string;
    userInputLabel?: string;
    passwordInputLabel?: string;
    checkBoxLabel?: string;
    loginErrorLabel?: string;
    loginTitle?: string;
    loginSubtitle?: string;
    forgotText?: string;
    submitLabel?: string;
    recaptcha?: boolean;
    apiUrl?: string;
    siteKey?: string;
    /**
    * The forgotPassword of the a element. Use this prop to make url accessible for screen readers..
    */
    forgotPassword?: string | (() => void);
    evaluateRecaptchaToken?: (token: string, apiUrl: string) => Promise<AxiosResponse<boolean>>;
    rememberMe?: boolean;
};
declare const SCFLogin: ({ onSubmit, forgotPassword, loginError, show, maxWidth, userInputLabel, passwordInputLabel, checkBoxLabel, loginErrorLabel, loginTitle, loginSubtitle, forgotText, submitLabel, evaluateRecaptchaToken, recaptcha, apiUrl, siteKey, rememberMe }: SCFLoginProps) => JSX.Element;
export default SCFLogin;
