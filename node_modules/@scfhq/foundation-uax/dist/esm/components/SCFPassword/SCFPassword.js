import React, { useState } from 'react';
import TextField from '@mui/material/TextField';
import { IconButton, InputAdornment } from '@mui/material';
import VisibilityIcon from '@mui/icons-material/Visibility';
import VisibilityOffIcon from '@mui/icons-material/VisibilityOff';
const SCFPassword = ({ show = true, newPasswordValidation, ...propsMaterial }) => {
    const [showPassword, setShowPassword] = useState(false);
    return (React.createElement(React.Fragment, null, show && (React.createElement(TextField, { ...propsMaterial, ...newPasswordValidation, type: showPassword ? 'text' : 'password', InputProps: {
            endAdornment: (React.createElement(InputAdornment, { position: "end" },
                React.createElement(IconButton, { onClick: () => setShowPassword(!showPassword) }, showPassword ? (React.createElement(VisibilityIcon, null)) : (React.createElement(VisibilityOffIcon, null)))))
        } }))));
};
export default SCFPassword;
