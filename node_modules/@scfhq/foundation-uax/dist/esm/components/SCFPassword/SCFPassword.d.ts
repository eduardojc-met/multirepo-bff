/// <reference types="react" />
import { TextFieldProps } from '@mui/material/TextField';
declare type SCFPasswordProps = TextFieldProps & {
    /**
    * Controls if the component must be rendered
    */
    show?: boolean;
    newPasswordValidation?: any;
};
declare const SCFPassword: ({ show, newPasswordValidation, ...propsMaterial }: SCFPasswordProps) => JSX.Element;
export default SCFPassword;
