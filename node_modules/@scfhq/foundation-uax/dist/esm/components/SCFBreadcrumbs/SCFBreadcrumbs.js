import React from "react";
import Breadcrumbs from "@mui/material/Breadcrumbs";
const SCFBreadcrumbs = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Breadcrumbs, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFBreadcrumbs;
