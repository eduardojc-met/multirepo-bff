import React from "react";
import Accordion from "@mui/material/Accordion";
const SCFAccordion = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(Accordion, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFAccordion;
