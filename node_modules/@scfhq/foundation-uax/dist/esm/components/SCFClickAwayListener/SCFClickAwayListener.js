import React from "react";
import ClickAwayListener from "@mui/material/ClickAwayListener";
const SCFClickAwayListener = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(ClickAwayListener, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFClickAwayListener;
