import React from "react";
import AppBar from "@mui/material/AppBar";
const SCFAppBar = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(AppBar, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFAppBar;
