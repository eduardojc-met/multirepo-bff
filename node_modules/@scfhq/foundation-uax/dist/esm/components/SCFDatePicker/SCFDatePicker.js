import React from "react";
import SCFInput from "../SCFInput/SCFInput";
const SCFDatePicker = ({ show = true, max = 100, min = 2, variant = "standard", form = undefined, rules = undefined, ...muiprops }) => {
    return (React.createElement(React.Fragment, null, show && (React.createElement(SCFInput, { ...muiprops, form: form, rules: rules, type: "date", InputProps: {
            inputProps: {
                max: max,
                min: min,
            },
        }, InputLabelProps: {
            shrink: true,
        } }))));
};
export default SCFDatePicker;
