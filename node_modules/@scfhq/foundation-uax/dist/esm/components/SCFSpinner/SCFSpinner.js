import React from 'react';
import CircularProgress from '@mui/material/CircularProgress';
const SCFSpinner = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(CircularProgress, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFSpinner;
