import React from "react";
import TreeView from "@mui/lab/TreeView";
const SCFTreeView = ({ show = true, ...muiprops }) => {
    return show === true ? React.createElement(TreeView, { ...muiprops }) : React.createElement(React.Fragment, null);
};
export default SCFTreeView;
