import React, { useState } from "react";
import Typography from "@mui/material/Typography";
import { SCFButton, SCFDialog, SCFDivider } from "../../components";
const SCFDialogDemo = () => {
    const [openDialog, setOpenDialog] = useState(false);
    const handleClickOpenDialog = () => {
        setOpenDialog(true);
    };
    const handleCloseDialog = () => {
        setOpenDialog(false);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Dialog"),
        React.createElement(SCFDivider, { sx: { mb: 4 } }),
        React.createElement(SCFButton, { variant: "outlined", onClick: handleClickOpenDialog }, "Open alert dialog"),
        React.createElement(SCFDialog, { open: openDialog, onClose: handleCloseDialog, "aria-labelledby": "alert-dialog-title", "aria-describedby": "alert-dialog-description", title: React.createElement("div", { className: "titulo-modal" }, "This is the Dialog Title"), content: React.createElement("div", null, "This is a Dialog component on the Foundations App, check the actions"), buttons: [
                { title: "cancel", onClick: handleCloseDialog },
                {
                    title: "TEST",
                    onClick: undefined,
                    variant: "contained",
                },
            ] })));
};
export default SCFDialogDemo;
