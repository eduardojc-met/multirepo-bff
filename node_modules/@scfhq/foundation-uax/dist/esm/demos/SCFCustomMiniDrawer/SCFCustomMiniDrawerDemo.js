import React, { useState } from 'react';
import { SCFButton, SCFCustomMiniDrawer, SCFDivider } from '../../components';
import { arrayMenuData } from './CustomMiniDrawerData';
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
export const SCFCustomMiniDrawerDemo = () => {
    const [open, setOpen] = useState(false);
    const handleDrawer = () => {
        setOpen(!open);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "CustomMiniDrawer"),
        React.createElement(SCFDivider, { sx: { mb: 4 } }),
        React.createElement(Grid, { container: true, spacing: 2, p: 4, direction: "column" },
            React.createElement(Grid, { item: true, alignContent: "center" },
                React.createElement(SCFButton, { onClick: handleDrawer }, "Open menu"),
                React.createElement(SCFCustomMiniDrawer, { arrayMenu: arrayMenuData, handleOpen: handleDrawer, handleRoutes: () => alert('rute'), handleSubOpen: handleDrawer, show: true, open: open })))));
};
export default SCFCustomMiniDrawerDemo;
