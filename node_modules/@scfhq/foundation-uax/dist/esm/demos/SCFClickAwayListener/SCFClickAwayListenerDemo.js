import React, { useState } from "react";
import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";
import { SCFButton, SCFClickAwayListener, SCFDivider } from "../../components";
const SCFClickAwayListenerDemo = () => {
    const [openClickAway, setOpenClickAway] = useState(false);
    const handleClickClickAway = () => {
        setOpenClickAway((prev) => !prev);
    };
    const handleClickAway = () => {
        setOpenClickAway(false);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Click Away Listener"),
        React.createElement(SCFDivider, { sx: { mb: 4 } }),
        React.createElement(SCFClickAwayListener, { onClickAway: handleClickAway },
            React.createElement(Box, { sx: { position: "relative" } },
                React.createElement(SCFButton, { type: "button", onClick: handleClickClickAway }, "Open menu dropdown"),
                openClickAway ? (React.createElement(Box, null, "Click me, I will stay visible until you click outside.")) : null))));
};
export default SCFClickAwayListenerDemo;
