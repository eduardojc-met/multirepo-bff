import React from "react";
import Typography from "@mui/material/Typography";
import { SCFMiniDrawer, SCFDivider } from "../../components";
import { Box } from "@mui/material";
import { menuData, menuData2 } from "./SCFMiniDrawerData";
const SCFMiniDrawerDemo = () => {
    return (React.createElement(React.Fragment, null,
        React.createElement(Box, null,
            React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "MiniDrawer"),
            React.createElement(SCFDivider, { sx: { mb: 4 } }),
            React.createElement(SCFMiniDrawer, { show: false, arrayMenu: menuData, arrayMenu2: menuData2 }))));
};
export default SCFMiniDrawerDemo;
