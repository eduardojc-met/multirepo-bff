import * as React from 'react';
import Box from '@mui/material/Box';
import Stack from '@mui/material/Stack';
import Slider from '@mui/material/Slider';
import VolumeDown from '@mui/icons-material/VolumeDown';
import VolumeUp from '@mui/icons-material/VolumeUp';
const SCFSliderDemo = () => {
    const [value, setValue] = React.useState(30);
    const handleChange = (event, newValue) => {
        /**
         * TODO MEJORAR
         */
        console.warn(event);
        setValue(newValue);
    };
    return (React.createElement(Box, { sx: { width: 200 } },
        React.createElement(Stack, { spacing: 2, direction: "row", sx: { mb: 1 }, alignItems: "center" },
            React.createElement(VolumeDown, null),
            React.createElement(Slider, { "aria-label": "Volume", value: value, onChange: handleChange }),
            React.createElement(VolumeUp, null)),
        React.createElement(Slider, { disabled: true, defaultValue: 30, "aria-label": "Disabled slider" })));
};
export default SCFSliderDemo;
