import React, { useState } from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import { SCFPopover, SCFDivider, SCFButton } from "../../components";
const SCFPopoverDemo = () => {
    const [anchorElPopover, setAnchorElPopover] = useState(null);
    const handleClickPopover = (event) => {
        setAnchorElPopover(event.currentTarget);
    };
    const handleClosePopover = () => {
        setAnchorElPopover(null);
    };
    const openPopover = Boolean(anchorElPopover);
    const id = openPopover ? "simple-popover" : undefined;
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Popover"),
        React.createElement(SCFDivider, null),
        React.createElement(Grid, { item: true },
            React.createElement(SCFButton, { "aria-describedby": id, variant: "contained", onClick: handleClickPopover }, "Open Popover"),
            React.createElement(SCFPopover, { id: id, open: openPopover, anchorEl: anchorElPopover, onClose: handleClosePopover, anchorOrigin: {
                    vertical: "bottom",
                    horizontal: "left",
                } },
                React.createElement(Typography, { sx: { p: 2 } }, "The content of the Popover.")))));
};
export default SCFPopoverDemo;
