import React, { useState } from "react";
import Typography from "@mui/material/Typography";
import { SCFBasicMenu, SCFButton, SCFDivider } from "../../components";
import { MenuItem } from "@mui/material";
const SCFBasicMenuDemo = () => {
    const [anchorEl, setAnchorEl] = useState(null);
    const openBasicMenu = Boolean(anchorEl);
    const handleClickBasicMenu = (event) => {
        setAnchorEl(event.currentTarget);
    };
    const handleCloseBasicMenu = () => {
        setAnchorEl(null);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Basic Menu"),
        React.createElement(SCFDivider, { sx: { mb: 4 } }),
        React.createElement(SCFButton, { id: "basic-button", "aria-controls": openBasicMenu ? "basic-menu" : undefined, "aria-haspopup": "true", "aria-expanded": openBasicMenu ? "true" : undefined, onClick: handleClickBasicMenu }, "Open Basic Menu"),
        React.createElement(SCFBasicMenu, { id: "basic-menu", anchorEl: anchorEl, open: openBasicMenu, onClose: handleCloseBasicMenu, MenuListProps: {
                "aria-labelledby": "basic-button",
            } },
            React.createElement(MenuItem, { onClick: handleCloseBasicMenu }, "Text Field 1"),
            React.createElement(MenuItem, { onClick: handleCloseBasicMenu }, "Text Field 2"),
            React.createElement(MenuItem, { onClick: handleCloseBasicMenu }, "Text Field 3"))));
};
export default SCFBasicMenuDemo;
