import React from 'react';
import TextField from '@mui/material/TextField';
const SCFTextField = ({ show = true, validate, ...muiprops }) => {
    return show ? React.createElement(TextField, { ...muiprops, ...validate }) : React.createElement(React.Fragment, null);
};
export default SCFTextField;
