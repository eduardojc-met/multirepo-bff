import React from "react";
import Typography from "@mui/material/Typography";
import { SCFDivider, SCFList } from "../../components";
import { ListItem, ListItemText } from "@mui/material";
const SCFDividerDemo = () => {
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Divider"),
        React.createElement(SCFDivider, { sx: { mb: 4 } }),
        React.createElement(SCFList, { "aria-label": "mailbox folders" },
            React.createElement(ListItem, { button: true },
                React.createElement(ListItemText, { primary: "Inbox" })),
            React.createElement(SCFDivider, { sx: { mb: 4 } }),
            React.createElement(ListItem, { button: true, divider: true },
                React.createElement(ListItemText, { primary: "Drafts" })),
            React.createElement(ListItem, { button: true },
                React.createElement(ListItemText, { primary: "Trash" })),
            React.createElement(SCFDivider, { light: true }),
            React.createElement(ListItem, { button: true },
                React.createElement(ListItemText, { primary: "Spam" })))));
};
export default SCFDividerDemo;
