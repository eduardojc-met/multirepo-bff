import React from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import MailIcon from "@mui/icons-material/Mail";
import { SCFBadge, SCFDivider } from "../../components";
const SCFBadgeDemo = () => {
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Badge"),
        React.createElement(SCFDivider, { sx: { mb: 4 } }),
        React.createElement(Grid, { container: true, spacing: 2, p: 4, direction: "column" },
            React.createElement(Grid, { item: true, alignContent: "center" },
                React.createElement(SCFBadge, { badgeContent: 4, color: "secondary" },
                    React.createElement(MailIcon, { color: "action" })),
                React.createElement(SCFBadge, { badgeContent: 4, color: "success" },
                    React.createElement(MailIcon, { color: "action" }))))));
};
export default SCFBadgeDemo;
