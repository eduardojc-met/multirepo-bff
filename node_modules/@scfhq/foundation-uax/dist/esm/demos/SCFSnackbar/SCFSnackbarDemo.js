import React, { useState } from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import { SCFSnackbar, SCFDivider, SCFButton } from "../../components";
const SCFSnackbarDemo = () => {
    const [open, setOpen] = useState(false);
    // Snackbar
    const handleClicSnackbar = () => {
        setOpen(true);
    };
    const handleCloseSnackbar = () => {
        setOpen(false);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Snackbar"),
        React.createElement(SCFDivider, null),
        React.createElement(Grid, { item: true },
            React.createElement(SCFButton, { onClick: handleClicSnackbar }, "Open simple snackbar"),
            React.createElement(SCFSnackbar, { open: open, autoHideDuration: 6000, onClose: handleCloseSnackbar, message: "Note archived" }))));
};
export default SCFSnackbarDemo;
