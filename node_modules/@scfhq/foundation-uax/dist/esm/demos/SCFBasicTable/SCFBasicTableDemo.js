import React from "react";
import Typography from "@mui/material/Typography";
import { SCFBasicTable, SCFDivider } from "../../components";
import { bodyData, headerData } from "./SCFBasicTableData";
const SCFBasicTableDemo = () => {
    const handleDetails = (row) => {
        alert(`'Vas a ver los detalles', ${row}`);
    };
    const handleDelete = (row) => {
        alert(`'Vas a borrar ' , ${row}`);
    };
    const handleEdit = (row) => {
        alert(`'Vas a editar', ${row}`);
    };
    const handleAdd = () => {
        alert("Vas a añadir");
    };
    const handleActive = (row) => {
        alert(`'Vas a reactivar a este usuario' ${row}'`);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "BasicTable"),
        React.createElement(SCFDivider, { sx: { mb: 4 } }),
        React.createElement(SCFBasicTable, { arrayHeader: headerData, arrayBody: bodyData, handleDetails: handleDetails, handleDelete: handleDelete, handleEdit: handleEdit, handleAdd: handleAdd, handleActive: handleActive })));
};
export default SCFBasicTableDemo;
