import React, { useState } from "react";
import Grid from "@mui/material/Grid";
import Typography from "@mui/material/Typography";
import { SCFSelect, SCFDivider } from "../../components";
const SCFSelectDemo = () => {
    const [selectValue, setSelectValue] = useState("D");
    const selectOptions = [
        { name: "Cat", value: "C" },
        { name: "Dog", value: "D" },
        { name: "Bird", value: "B" },
    ];
    const handleChange = (event) => {
        console.warn(event);
        setSelectValue(event.target.value);
    };
    return (React.createElement(React.Fragment, null,
        React.createElement(Typography, { variant: "h1", gutterBottom: true, fontWeight: "bold", sx: { mt: 8 } }, "Select"),
        React.createElement(SCFDivider, null),
        React.createElement(Grid, { item: true, alignContent: "left" },
            React.createElement(SCFSelect, { id: "demo-simple-select", label: "Choose one", name: "Pet", value: selectValue, sx: { width: "300px" }, variant: "filled", required: false, onChange: handleChange, options: selectOptions }))));
};
export default SCFSelectDemo;
